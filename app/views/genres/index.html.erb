<p style="color: green"><%= notice %></p>

<h1 class="title is-1">Genres</h1>
<%= paginate @genres %>
<%= page_entries_info @genres %>
<div><br></div>
<div id="genres">
  <% @genres.each do |genre| %>
    <%= render genre %>
    <p>
      <%= link_to "Show this genre", genre %>
    </p>
    <br>
  <% end %>
</div>

<%# <%= link_to "New genre", new_genre_path %>
