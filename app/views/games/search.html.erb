<div class="columns">
  <div class="column">
    <h2 class="title is-2"><%= params[:keywords] %> </h2>
    <ol class="ml-5">
      <% @games.each do |game| %>
        <li>
          <%= link_to game.name, game %>
          <em>(<%= game.genre.name %>)</em>
        </li>
      <% end %>
    </ol>
  </div>